# Dockerfile para el servidor de prueba
FROM golang:1.25-alpine

WORKDIR /app

# Copiar solo el servidor de prueba
COPY test-server.go .

# Exponer puerto
EXPOSE 8080

# Ejecutar servidor
CMD ["go", "run", "test-server.go"]
